<div class="p-3">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <br>
    <div id="searchResult"></div>
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}
</div>
